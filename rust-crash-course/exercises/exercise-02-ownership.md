# Exercise 02 — Ownership Practice

Write small functions that:

- Take ownership of a `String` and return its length.
- Borrow a `&str` and return its length.

Put sample calls in `solutions/solution-02-ownership.rs` and ensure they compile.
